# Golang Product

## Methods

### CLI
**CLI** Calls the `go version` and reads version from output.

**Arguments** - NONE

### File
**File** Reads the `go.mod` from file specified at PATH argument.

**Arguments**
| Argument | Type | Description |
| -------- | ---- | ----------- |
|  Path | String | Path where to call command |