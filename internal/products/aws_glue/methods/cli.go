package aws_glue_methods

type CLI struct{}

func (c CLI) Run(id string, args []any) (string, error) {
	// TODO: implememt calling AWS CLI
	// requires job name arg
	// can this be done with SDK too?
	return "", nil
}
